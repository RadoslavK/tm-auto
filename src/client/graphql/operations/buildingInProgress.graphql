#import "./misc.graphql"

query GetBuildingsInProgress($villageId: Int!) {
    buildingsInProgress(villageId: $villageId) {
        level
        finishedAt {
            ...Timestamp
        }
        name
        type
    }
}
