input UserAccountInput {
    username: String!
    password: String!
    server: String!
}

type Query {
    isSignedIn: Boolean!
}

type Mutation {
    signIn(account: UserAccountInput!): Boolean!
    signOut: Boolean!
}

extend type Subscription {
    signedToggled: Boolean!
}
