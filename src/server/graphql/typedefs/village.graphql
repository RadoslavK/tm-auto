type VillageCapacity {
    granary: Int!
    warehouse: Int!
}

type VillageResources {
    amount: Resources!
    capacity: VillageCapacity!
    production: Resources!
}

type Village {
    id: Int!
    coords: Coords!
    name: String!
    resources: VillageResources!
    isCapital: Boolean!
}

extend type Query {
    village(villageId: Int!): Village
    villages: [Village!]!,
}

extend type Subscription {
    updateVillage: Boolean!
    updateVillages: Boolean!
}
