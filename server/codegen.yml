overwrite: true
schema: "http://localhost:3000/graphql"
documents: null
generates:
  src/_types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      typesPrefix: I
